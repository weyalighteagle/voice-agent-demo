# Relay server WSS URL — will be set as Railway env var at build time

# Format: wss://your-relay-server.up.railway.app

# This is intentionally left blank here — set VITE_RELAY_WSS_URL in Railway dashboard

VITE_RELAY_WSS_URL=
